{
 "engine_v": "R54",
 "frozen": "2026-08-19",
 "plaintext_sha256": "35c9bb7037abd77fed7a07962aa4739b0ec619c7238a008015ffeffe6a3bd3bc",
 "plaintext_utf8_bytes": 342565,
 "plaintext_chars": 340650,
 "markers": 10,
 "marker_names": [
  "AGES",
  "CFG",
  "EQPO",
  "GOALS",
  "HANDOFF",
  "RAW",
  "SAGEH",
  "STATE",
  "TPM",
  "VARS"
 ],
 "sageh": {
  "baked_fallback": true,
  "nightly_splices_fresh": true,
  "source": "Yearly Goals.xlsx 'Sage GL History' tab — read fresh every run per SKILL.md §2c; baked payload is only the fallback (never splice null). R35: optional disc ($, GL-negative) + gross series from the Discount % column. R37: optional nrMgn / cbMgn / wtyMgn series — the Non-Rev / Callback / Warranty Margin columns, dollars exactly as the Summary card's Margin row (negative = loss; blank → 0; all three to BOTH trades or to neither). The R36 nrEro/cbEro/wtyEro keys are RETIRED — no Excel column ever carried them. The Trends margin section hides when a payload lacks the keys"
 },
 "june_parity": {
  "jobs": 3902,
  "revenue": 6087636,
  "gm": "55.3%",
  "window": "06/01/26 - 06/30/26",
  "caveat": "R38: membership revenue moves revenue/GM whenever goals.json carries mem keys — June parity holds ONLY when the goals payload has no mem block. R41 changed FLAG CLASSIFICATION only. R42 adds assumed install pay only on Install-bucket jobs ≤14 days old at the data-snapshot moment — mature-month rebuilds byte-identical (verified). R43 paces the three fixed monthly $ (Holiday Pay / PTO / onboarding × $4,000) by the engine's elapsedFrac — on a CLOSED month elapsedFrac=1, paced = full monthly, and the June A/B vs R42 is byte-identical (RAW modulo builtAt; 12/12 rendered panes across 4 tabs × 3 scopes; every pre-existing summary key; verified 2026-08-05 with the baseline fixed values loaded). A YOUNG window legitimately moves: Direct Labor carries only the elapsed share of the fixed $ (MTD = monthly × elapsedFrac), so mid-month GM/CP read higher — and more honest — than R42, and the Pace view carries exactly the full monthly fixed $ instead of inflating it by ÷ elapsedFrac. That is the point of R43, not drift. R44 is DISPLAY-ONLY (full-screen job card + project rollup, built on row click from the engine's own per-job records) — it cannot move any number: June A/B vs R43 verified 12/12 rendered panes byte-identical, RAW byte-identical (modulo builtAt), every summary key identical. R45 adds a per-system multiplier to the assumed install pay — it can only move jobs <=14 days old at the data-snapshot moment, so the mature-June rebuild is unchanged to the cent (verified: jobs, flag set, per-job rev/purch/labor/GM/instTop and all 71 pre-existing summary keys x 3 scopes identical; 9/12 rendered panes byte-identical, the 3 Summary panes differ ONLY by the Reconciliation label gaining 'x systems sold on the invoice'). RAW is NOT byte-identical vs R44: parseDisc now serializes a per-job `units` map (+9,745 bytes on June) — this is the one intended payload change and it is what makes the multiplier survive a saved copy. R46 is DISPLAY-ONLY (ServiceTitan deep link on every job number) and adds an OPTIONAL parser field: the 'Job ID' column of the Job Costing export is captured as job.stid and the key is ATTACHED ONLY when a real id was supplied, so an export predating the column serializes RAW byte-for-byte identically to R45. Verified on June 2026: RAW byte-identical (modulo builtAt), all 7 other data markers identical, 12/12 rendered panes (4 tabs x 3 scopes) byte-identical, every per-job record identical, every non-jobs result section identical, snap.js reproduces the ledger exactly (171 flagged / 126 HVAC / 45 Plumb / $299,421)."
 },
 "release_rule": "A template revision is NOT released until, in the SAME session: (1) this template.html + manifest are re-frozen in the repo, (2) the skill bundle is re-saved with the matching pp_template.html and SKILL.md, (3) the nightly task prompt carries no stale revision facts. Nightly: if the skill folder SKILL.md states a HIGHER revision than this manifest, that is a broken release — STOP and report; never silently publish the older engine.",
 "notes": "R44 (2026-08-06, per Stephen): FULL-SCREEN JOB CARD + PROJECT ROLLUP - 'on the jobs tab, when you expand a job, the labor breakdown is condensed so you cant see the numbers... that table only expands partially, it should fill the screen... since it has the job# and project# it can connect jobs to projects... we need to know what the total revenue, discount, labor %, purchase%, Gross margin %, Gross Profit $ is for all the jobs on the project combined.' Three fixes, all display-only. (1) The old inline <tr class=det> expansion rendered INSIDE table.jobs, so nested cells inherited white-space:nowrap and the long labor-build labels overlapped/clipped the numbers (the Techs line clipped mid-name at 1600px). (2) It sat inside the gridwrap 70vh scroll box, so it only ever opened partially. Clicking a job now opens a fixed full-screen overlay card (.jdov, body-level, outside the table): waterfall / labor build (assumed-vs-actual COM and install plan-vs-posted inline on wrapped readable lines + a Direct Labor total row) / commission derivation, close via X, Esc or backdrop click. (3) When the job carries a Project #, the card adds the Project rollup: every sibling job on that project in the loaded month (revenue-desc rows: rev, counting disc, labor $ %, purchases $ %, GM % $, current job highlighted, member rows click through to their own card) plus six combined tiles - Total revenue, Discounts (+ project disc % of book -> comm rate off the engine's own project tier), Labor %, Purchases %, GM % (gm-banded pill), GP $ - because one compliant project sale is often split across several jobs/crews (dig/drywall/pipe/backfill) and one job can margin poorly while the PROJECT as sold is healthy. Aggregation is pure display arithmetic over the engine's per-job records (same convention as the grid TOTAL row); the engine's project grouping/tier math (computeAll 'projects' map) is UNTOUCHED. Notes state the month-window caveat, the subs/permits-in-GM reconciliation and benign-credit exclusion. buildSaveHtml splices PAGE0 so an open card never serializes into a saved copy. Verified 2026-08-06 (jsdom, June exports, frozen build.js, goals=skip): gates 6/6 both engines, 3,902 jobs / $6,087,636 / GM 55.3% exact, RAW byte-identical (builtAt normalized), 12/12 rendered panes (Summary+recon/P&L/Jobs/Trends x Branch/HVAC/Plumb) byte-identical, every summary key identical; 39/39 A/B+smoke checks green incl. combined stats reconciling to independent sums to the cent on a live 5-job project (P627979490: $25,333 rev / $1,333 disc / 24.8% labor / 11.3% purch / 63.5% GM / $16,095 GP), member click-through, Esc/X/backdrop close, solo-project note, no-project card without the section, save round-trip carrying R44 code with no live overlay markup; Chromium screenshots confirm the old clipping and the new card. June data carries 540 multi-job projects. Whole-file diff vs R43 = 10 intended hunks (overlay+pjsec CSS, mobile CSS, header/footer stamps, footer changelog, toggleDetail->showDetail/pjRollup, Instructions section 9). build.js and close.js run VERBATIM.  || R45 (2026-08-07): Review Desk repaint fix + reflag-on-sync + per-system HVAC install assumption + rebuilt Pay Plan tab. Reproduced the desk failure end-to-end in jsdom against a wired CFG with a mocked private-repo state.json: sync pulled 5 approvals / 3 watch / 8 log entries, and the R44 desk still reported 'No review activity recorded yet' with 0 approved / 0 watchlist while the Jobs tab's own pills read 'Approved (0) | Watchlist (0)' until clicked, at which point they immediately corrected to 'Approved (5) | Watchlist (3)' — the exact asymmetry reported. On R45 the desk populates before any click (HVAC 3 approved / 1 watch, Plumbing 2 / 1, reviewer table AG/RP/TR/EO, open queue 171 -> 164) and repaints again on tab open. A/B vs R44 on mature June: jobs 3,902 / $6,087,636 / GM 55.3%, flag set identical (171), per-job rev+purch+labor+GM+instTop identical, all 71 pre-existing summary keys x 3 scopes identical, 9/12 rendered panes byte-identical (the 3 Summary panes differ only by the Reconciliation label). RAW grows 9,745 bytes for the new disc `units` map — the single intended payload change. snap.js + ages.py run verbatim on R45 and reproduce the R44 ledger exactly. gates 6/6.",
 "mem": {
  "source": "Yearly Goals.xlsx 'Margin Variables' tab — 'Other Revenue' block (Membership row: Revenue $ + Purchases $, branch monthly totals)",
  "split": "94% HVAC / 6% Plumbing, applied at ingest into PP_GOALS[trade].mem={rev,cost}",
  "behavior": "MTD = monthly x calendar elapsedFrac; pace carries the full monthly figure exactly; cost lands in Purchases; all margins/% re-base to TOTAL revenue; mem absent/invalid → renders exactly R37"
 },
 "r39": {
  "caps": "service-side 480 min/job, install 720 min/job; CB/WT worked entirely by an install crew inherit the install cap",
  "time_source": "Job Costing 'Start of Working Time' / 'End of Working Time' x crew count from 'Assigned Technicians'; bucket median backfills the ~2% of jobs with no usable stamps",
  "rate": "scope GP (and CP) split between service and install by share of job-attributed margin, divided by that side's man-minutes; lost minutes priced by the side of the tech who logged them",
  "caveat": "opportunity cost at current productivity - not recoverable cash"
 },
 "r40": {
  "attribution": "by trade, never by service manager - SMs help sell the job and do not review",
  "private_store": "GTL-Ops-652/GTL-652-PP-Private - review/state.json + ledger/flag_ages.json + ledger/flag_snapshots/",
  "token_scope": "fine-grained Contents read+write on the private repo ONLY - verified: write to the public pages repo returns 403",
  "aging_caveats": "capped=true ages are floors (rendered >=Nd); cost-maturity inflates 0-2d flags; approval does NOT clear a flag in the data, so age alone cannot separate 'never looked at' from 'reviewed and accepted'",
  "gm_gate": "?gm=1 latches pp_gm in that browser - a visibility choice, NOT a security boundary"
 },
 "r41": {
  "mem_classification": "job.memOver = comOver AND memRev>0 AND (actualCOM-assumedCOM) <= 12% x memRev + $1; memRev = positive invoice items matching /sadie|member/ (name or code), classification only — the pay base still excludes exactly the sadie items; memRev falls back to sadieRev when RAW predates R41",
  "no_pay": "job.noPay = rev>=1000 AND zero COM/COMUT/BON/hourly — info field + dashed chip, never a flag (R42: young install jobs listed under NO-PAY already carry the assumed book plan in margin)",
  "ledger_rebase": "first R41 nightly: flagged count drops (June rebase 293→171; Aug 1–3 rebase 48→7) because membership-only comm-overs and NO-PAY leave the flag set — ledger entries for those jobs stop updating (last_seen freezes); intended re-baseline, not data loss",
  "raw_compat": "RAW.disc.byJob[*].memRev is the one new serialized key; R40-era saved copies classify via the sadieRev fallback — only a non-sadie membership wording (none observed in June) would be missed until the file is re-parsed"
 },
 "r42": {
  "rule": "Install-bucket job, rev>0, age ≤14d (vs mat.asOf, End-of-Working-Time fallback Start; no usable stamp → no assumption) → instAssumed = HVAC flat-rate book by job type / Plumb 8% of job revenue; instTop = instAssumed>0 ? max(0, instAssumed − actual COMUT) : 0; labor += instTop; day 15+ → actuals only",
  "instdef_hvac": {
   "H-Install Split Horizontal": 1350,
   "H-Install Split Vertical": 1350,
   "H-Install Split Gas": 1350,
   "H-Install Split Heat Pump": 1400,
   "H-Install Package": 1000,
   "H-Install Mini-Split": 1000,
   "H-Install Duct-System": 1300,
   "H-Install Partial": 600,
   "H-Install Clean, Seal, IAQ": 161,
   "H-Install Insulation": 226
  },
  "plumb_pct": 0.08,
  "age_max_days": 14,
  "accrual_netting": "n = min(pendPay, instTop) per young job; pendPay −= n; acc −= n; accrMat = accr − accrPay invariant — no double count with the Cost-Maturity pay component; the Yearly Goals Cost Maturity block needs NO change",
  "provenance": "rates = median crew flat-rate pay per job type, Q2-2026 payfiles (Apr/May/Jun medians identical; 86–100% of jobs of these types carry the pay); Plumb 8% exact on 533/576 Q2 install jobs; excluded types (Parts 37% paid, Drywall, QCI) pay too irregularly to assume — re-measure INSTDEF when the install pay plan changes and cut a revision",
  "raw_compat": "no new RAW keys — pre-R42 saved copies and reused nightly payloads compute R42 rules on reopen; their builtAt anchors the ages, so an old file keeps its as-built assumption"
 },
 "r43": {
  "rule": "holiday = holidayM × elapsedFrac; pto = ptoM × elapsedFrac; onboardPay = onboardN × 4000 × elapsedFrac — inside Direct Labor, per scope; elapsedFrac = the engine's R29 elapsed-day fraction (through yesterday, capped by the window)",
  "pace_identity": "Pace = MTD ÷ elapsedFrac, so the paced P&L carries exactly the full monthly fixed $ — pre-R43 pace carried fixed ÷ elapsedFrac (inflated)",
  "display": "P&L rows + Reconciliation annotate 'NN% elapsed of $X/mo' when elapsedFrac<1; plain labels on closed months (byte-identical to R42); Variables labels now read '$/mo — paced to elapsed days'",
  "summary_keys_added": [
   "holidayM",
   "ptoM",
   "onboardPayM"
  ],
  "untouched": "job-level math, flags (verified identical sets), accrual + accrual netting, instTop, Handoff, RAW format, review machinery",
  "raw_compat": "no new RAW keys — pre-R43 saved copies pace on reopen via their embedded VARS monthly values (same reopen semantics as overhead pro-ration)"
 },
 "r44": {
  "ux": "row click opens a fixed full-screen overlay card (.jdov) OUTSIDE table.jobs - kills the inherited white-space:nowrap overlap AND the 70vh partial-expand; close via X / Esc / backdrop; mobile media block makes the pane 100vw/100vh",
  "project_rollup": "shown when job.proj is set: members = S.result.jobs filtered on proj (both trades, revenue-desc); combined tiles Total revenue / Discounts (+project discPct of book -> comm rate from the engine's own tier) / Labor % / Purchases % / GM % / GP $; per-member rows rev, counting, labor $ %, pur $ %, GM % $ + PROJECT TOTAL row; solo-project note when members==1; caveat: loaded data month only",
  "aggregation": "pure display sums of engine per-job fields (rev/counting/benign/labor/pur/sub+permit/gm) - identical convention to the Jobs grid TOTAL row; no math re-implemented, computeAll untouched",
  "labor_build": "assumed-vs-actual COM and install plan-vs-posted now inline on the COM/COMUT rows (wrapped, readable); new Direct labor total row ($ + % of revenue)",
  "untouched": "parsers, computeAll, buckets, flags, accrual + netting, Handoff, Trends, review machinery, renderJobs (Jobs pane byte-identical) - RAW format unchanged, no new serialized keys",
  "raw_compat": "pre-R44 saved copies reopen unchanged (their embedded engine renders the old inline row); files saved FROM R44 carry the card + rollup"
 },
 "r45": {
  "review_desk_fix": "THE bug: `const PPUI = (()=>{...})()` binds in the global LEXICAL environment, which is NOT a property of window, so STX.pull()'s completion guard `if(window.PPUI&&PPUI.stateChanged)` evaluated undefined on EVERY load since R17 and the post-sync repaint never fired. Under a wired CFG, STATE0 ships empty (SKILL 3c), so the desk painted once against empty state and stayed there. The Jobs tab masked it — its view pills, filter chips and column sorts all call renderJobs(), so Approved/Watchlist self-healed on first click, which is exactly the symptom Stephen reported. Fix: `window.PPUI=PPUI;` after the IIFE, plus setTab() now calls renderDesk() when the desk tab is opened (independent second path).",
  "reflag_fix": "checkReflags() ran only under recompute(fromIngest) — i.e. only on a manual file drop. On the nightly site every viewer loads RAW and never drops a file, so approved-then-went-bad was never detected and the desk's 'Approved then went bad' row could not light up. stateChanged() now runs it once when synced state lands, guarded to nightly data (!S.manual, pay+disc present). Idempotent: it consumes the approval it reflags. Verified: honest snapshots survive with ZERO reflags and ZERO state PUTs; stale snapshots reflag correctly.",
  "install_per_system": "HVAC assumed install pay is now book rate x systems sold. Count comes from parseDisc: priced lines with Item GL Group Name 'HVAC- Replacement', summed by Item Quantity into families sp/pk/ms (mini tested FIRST — 'Mini-Split' contains 'split'); blank/zero quantity on a priced line reads as 1. INSTDEF.fam limits it to the six EQUIPMENT-system job types (4 Splits, Package, Mini-Split); INSTDEF.qtyCap=6. Duct-System, Partial, Insulation and Clean/Seal/IAQ are DELIBERATELY excluded — their invoice quantity is inches / square footage / scope, not a unit count (measured: an Insulation job reads quantity 6 against $228 of real pay, which would over-assume by $1,128). Plumbing needs no multiplier — 8% of revenue already scales with the sale.",
  "install_verification": "June 2026: 127 priced system lines parsed; 11 of 326 HVAC book-rate install jobs are multi-system (nine 2-system, two 3-system, 3.4%). Exact book-vs-posted agreement rises 151 -> 160 of 315 paid jobs with ZERO new over-assumptions (the only 2 over-assumed jobs were already over-assumed at x1). Synthetic young clock (as-of 2026-07-01) with install pay zeroed on the 7 in-window multi-system jobs: R44 assumes $9,100, R45 assumes $20,900 — $11,800 previously invisible; worst single job 544995 (3 systems, $30K) read GM 58.1% under R44 vs the honest 49.1%. Flag set unchanged (171 both), pending-materials accrual identical to the cent (the R42 netting invariant holds).",
  "pay_plan_tab": "Variables -> Pay Plan rebuilt as documentation a newcomer can follow: 4 quick-reference cards, pay-code decoder, commission ladder + discount-cliff bar chart, HVAC flat-rate book table with a 'x systems?' column beside Plumbing's flat 8% + effective-%-of-revenue bar chart, 4 worked examples, assumed-install timeline (day 0-14 floor -> day 15 actuals), payroll calendar + stub-length table, pay-code landing-curve chart, and a sourced June-2026 verification block. Static HTML + a scoped .pp* CSS block; no script, no bindings, no engine contact.",
  "summary_keys_added": "instMultiN (jobs with a top-up AND >1 system), instMultiSys (total systems on those jobs) — both 0 on a mature month.",
  "untouched": "parsers otherwise, project/tier math, buckets, GM/CP, accrual vector + R42 netting, Handoff, Trends, flag definitions, renderJobs, the job card and project rollup. build.js, close.js, snap.js and ages.py run VERBATIM (snap.js on R45 reproduces 171 flagged / 126 HVAC / 45 Plumb / $299,421, identical to R44).",
  "raw_compat": "Pre-R45 RAW (saved copies, reused nightly data) carries no `units` — computeAll falls back to x1, i.e. exactly R42/R44 behavior. Files saved FROM R45 carry the counts and keep the multiplier."
 },
 "r46": {
  "what": "Every job number is a link into ServiceTitan. Rendered at four sites: the Jobs-grid job cell, the job-card header (R44 overlay), every member row of the Project rollup, and the Review Desk open-flag queue.",
  "source": "The OPTIONAL 'Job ID' column of the Job Costing w/ Project info export (added by Stephen 2026-08-07). This is the ServiceTitan INTERNAL job id (9-digit, e.g. 644584905) and is a DIFFERENT number from the 6-digit Job # (e.g. 552304). Only the internal id can address a job in the ServiceTitan UI.",
  "url": "https://goettl_lasvegas.eh.go.servicetitan.com/#/Job/Index/<stid> — same pattern already frozen in the QA Dashboard skill (qa_v1_1), where the ids were cross-checked against Databricks prd.gold_core_model.dim_job_info.job_id: 1783 matched, 0 mismatched.",
  "parser": "stIdOf() accepts digits only, 6-12 long, after stripping whitespace and commas; anything else -> null. parseJobs attaches rec.stid ONLY when non-null. Column resolution is by header NAME (the engine's existing findHeader/H map), never positional.",
  "render": "stLink(j) returns <a class=\"stl\" href=... target=\"_blank\" rel=\"noopener noreferrer\" onclick=\"event.stopPropagation()\">Job#</a> when j.stid is set, else the bare job number. stopPropagation is MANDATORY: two of the four call sites sit inside a clickable row (Jobs grid opens the job card, project-rollup member row navigates) and without it a click would follow the link AND fire the row handler. CSS: .stl med-blue, dotted underline, small superscript arrow affordance.",
  "degrade": "A missing 'Job ID' column, a blank cell, or a non-numeric value is a SOFT degrade — the job number renders as plain text exactly as R45. Never a stop, never a flag, never a gate failure.",
  "untouched": "parsers otherwise, computeAll, project/tier math, buckets, GM/CP, flags, accrual + R42 netting, install-pay assumption, Handoff, Trends, review machinery. build.js, close.js, snap.js and ages.py run VERBATIM.",
  "raw_compat": "Pre-R46 RAW (saved copies, reused nightly payloads) carries no stid — every job renders as plain text, i.e. exactly R45. Files saved FROM R46 carry the ids and keep the links.",
  "verification": "Verified 2026-08-07 (jsdom, June 2026 exports through frozen build.js, goals=skip). Zero-drift A/B vs R45: gates 6/6 both, 3,902 jobs / $6,087,636 / GM 55.3% / window 06/01/26-06/30/26, RAW byte-identical, 7/7 other markers identical, 12/12 panes byte-identical, 3,902/3,902 per-job records identical, zero anchors rendered (June export has no Job ID column). Link build (June spine with a synthetic Job ID column, 10% deliberately blank): 3,512 of 3,902 jobs carry a valid id, 1,350 Jobs-grid anchors all with target=_blank + rel=noopener, every href resolving to that row's own id, 150 rows correctly plain; job-card header linked; 2/2 project-rollup member rows linked with row click-through still navigating; Review Desk 152 of 171 queue rows linked; save round-trip carries stid, serializes no live overlay, stamps R46, and the reopened copy re-renders all 1,350 links. Real August 1-7 export parsed: 785 of 785 real job rows carry a valid Job ID (the 786th is the grand-total row, filtered by the existing BU rule). StatiCrypt gate: zero plaintext leaks (buildSaveHtml, four real job numbers, 'servicetitan.com', 'Job/Index' all absent from the ciphertext)."
 },
 "r47": {
  "what": "Approve / Watchlist actions inside the R44 full-screen job card. Per Stephen 2026-08-07: 'on the jobs tab, when I expand a job, add the watchlist and approve button to the top right of the expanded box, so a job can be added to the watchlist, or be approved from there. and if a job is added to either approved, or the watchlist from that screen, then close the expanded box.'",
  "render": "cardReview(j) builds the button group; showDetail() wraps it plus the existing close button in <div class=\"rvg\"> at the right of the .hd header (margin-left:auto). State-driven: active -> [Approve if j.flags.length||ST.reflag[job]] + [Watchlist]; watch -> 'Watching BY M/D' + [Approve] + [Remove from watchlist]; approved -> 'Approved BY M/D' + [Undo approval]. Same .ab / .ab.ok / .ab.rm classes as the grid, sized up (12px) for the card.",
  "approve_scope": "Approve is offered ONLY where the Jobs-grid review column offers it (a flagged or re-flagged job) - approving an unflagged job would record an approval that clears nothing. Watchlist is offered on ANY job (per Stephen): parking a clean job you want to keep an eye on is the point of a watchlist. This is the one intentional divergence from reviewCell().",
  "close_rule": "doAction() now RETURNS true only when STX.record() actually persisted a state change (approve / watch / unapprove / unwatch); it returns false on a cancelled initials or note prompt, on the disabled manual-data path, and on every note action. The card handler is `if(doAction(a,job)) closeDetail();` - so a recorded action closes the card and a cancel leaves it open. The Jobs-grid handler ignores the return value and is byte-identical in behaviour.",
  "one_workflow": "Same STX actions, same ST state model, same initials() stamp, same confirm() prompts and the same STX.wired()&&S.manual disabled rule as the grid. renderJobs() still runs inside doAction, so the view pills (Approved (n) / Watchlist (n)), the grid rows and the Review Desk all move together. No second state store, no second code path.",
  "untouched": "parsers, computeAll, project/tier math, buckets, GM/CP, flags, accrual + R42 netting, install-pay assumption, Handoff, Trends, checkReflags, PP_REFLAG, STX, buildSaveHtml. build.js, close.js, snap.js and ages.py run VERBATIM.",
  "raw_compat": "No RAW / STATE format change whatsoever. Pre-R47 saved copies reopen unchanged; a copy saved FROM R47 carries the same STATE shape it always did.",
  "verification": "Verified 2026-08-07 (jsdom + Chromium, June 2026 exports through frozen build.js, goals=skip). Zero-drift A/B vs R46: gates 6/6 both, 3,902 jobs / $6,087,636 / GM 55.3% / window 06/01/26-06/30/26; ALL 8 data markers byte-identical incl. RAW (1,223,163 chars, modulo builtAt); 21/21 rendered panes (7 tabs x 3 scopes) byte-identical modulo the R46->R47 stamp and the build clock. Functional 28/28: flagged card exposes Approve+Watchlist; Watch records and closes; reopened watched job shows 'Watching SG' + Approve + Remove and no duplicate Watch; Approve records, closes, moves Watchlist 1->0 and Approved 0->1; reopened approved job shows 'Approved SG' + Undo only; Undo records and closes; a cancelled prompt records nothing and leaves the card open; a clean job offers Watchlist but not Approve; Esc still closes; buildSaveHtml body carries no live overlay and stamps R47; zero engine alerts. Chromium screenshots confirm the three header states and a clean wrap at 430px."
 },
 "r48": {
  "what": "Duct-System and Partial join the assumed-install-pay multiplier. Per Stephen 2026-08-12: 'if something pays flat rate, and they do 5 they get 5 times... thats all job types, and nobody in the world would ever think otherwise.'",
  "why_r45_was_wrong": "R45 excluded Duct-System, Partial, Insulation and Clean/Seal/IAQ on the stated grounds that their invoice quantity is inches/square footage/scope rather than a unit count. Measured against Q2-2026 (Apr-Jun, 217 paid Duct/Partial jobs) that is FALSE for Duct-System and Partial: every priced 'HVAC- Replacement' line on those tickets is one discrete piece of equipment at quantity 1 ('3 Star Duct (4-5 Ton)' n=36 qmin=qmax=1; '1-Star, 4-Ton AC Condenser' n=22; '5 Ton Cased Coil' n=6), so the LINE COUNT is the piece count. Payroll pays the multiple: 2-duct jobs paid exactly $2,600 (2 x $1,300), a 3-piece Partial exactly $1,800 (3 x $600).",
  "counting": "New family 'eq' in dim units = every priced HVAC- Replacement line on the ticket (NOT bucketed to one equipment family, because a Partial mixes condensers, coils, air handlers and duct on one job). Per-line quantity above INSTDEF.qtyCap reads as 1 (an area/length measure, never a piece count); NONUNIT_RE excludes labor-warranty lines. INSTDEF.fam maps Duct-System and Partial to 'eq'.",
  "steps": "INSTDEF.steps overrides book x count where payroll uses a BANDED piece-count rate. Partial measured Q2: 1 piece $600 (53 of 81 jobs exact), 2 pieces $1,000 (21 of 31 exact - NOT $1,200), 3 pieces $1,800. steps={'H-Install Partial':{2:1000}}; counts outside the table fall through to book x count.",
  "still_excluded": "Insulation and Clean/Seal/IAQ. There the R45 reasoning HOLDS: blown-in insulation bills square footage (850-2,600 per line), and neither carries a real flat rate - 29 distinct pay amounts across 69 Insulation jobs, 78 across 199 Clean/Seal jobs. Their $226/$161 is a measured MEDIAN, not a book rate, so there is nothing to multiply. Fixing those requires the actual HVAC install flat-rate sheet.",
  "raw": "parseDisc now writes units.eq on any job with a priced HVAC- Replacement line. On June this ADDS 36 byJob entries that carry nothing but units.eq (every other field at its default, i.e. numerically inert - discBy already defaults to exactly that zero object) and leaves all 821 pre-existing entries byte-identical once eq is dropped. Pre-R48 saved copies have no eq key -> falls back to x1 = exactly R47 behavior.",
  "verified": "2026-08-12, jsdom, June 2026 exports through frozen build.js, goals=skip. Mature-June A/B vs R47: gates 6/6 both, 3,902 jobs / $6,087,636 / GM 55.3% / window 06/01/26-06/30/26, ALL 3,902 per-job records identical, all summary keys identical in all 3 scopes, 15/21 rendered panes byte-identical and the 6 that differ are only Summary (reworded Reconciliation sentence - identical once normalized) and Variables (rewritten Pay Plan tab, static HTML); 7 of 8 data markers byte-identical, RAW differs only by the new eq key. Flags 171 unchanged; snap.js reproduces the ledger exactly (171 flagged / 126 HVAC / 45 Plumb / $299,421.09). Synthetic young clock (instAsOf = 2026-06-25): exactly 12 jobs move - 11 Partial (ten 2-piece at the banded $1,000, one 3-piece at $1,800) and 1 Duct-System (2 systems at $2,600) - adding $705 of assumed labor; against posted pay 7 of 11 Partial plans land EXACT, 4 posted above plan (floor inert), 1 Duct-System plan above posted (the honest young-window floor). Flag count unchanged at 172 on the young clock. build.js, close.js, snap.js and ages.py run VERBATIM.",
  "measurement_source": "Q2-2026 ServiceTitan exports (Job Costing / Real Installer Pay / Real Discounts, 04/01/26-06/30/26) held in the Claude.ai project."
 },
 "r50": {
  "date": "2026-08-17",
  "title": "Approve is now Reviewed; undo is now Re-Flag",
  "requested_by": "Stephen 2026-08-17: rename the approve button to Reviewed, rename the undo-approval button to Re-Flag, and rename Approved to Reviewed anywhere it appears.",
  "scope": "Wording only. 24 user-facing strings across the Jobs grid, the R47 job card, the view pill, the confirm dialog, the re-flag reason strings and the Review Desk. Data keys, STX actions (approve/unapprove), the state.json shape, the sync protocol and the re-flag snapshot logic are UNTOUCHED - every review already recorded carries over unchanged.",
  "preserved": "The footer changelog keeps its historical R17/R18/R32/R47 wording (those entries describe what those revisions did); an R50 entry is appended instead.",
  "naming_note": "Re-Flag reuses the word the engine already applies to AUTOMATIC re-flagging (checkReflags, the N RE-FLAG pill, the desk red row). A manual Re-Flag removes a review and does NOT increment that counter. Flagged to Stephen 2026-08-17.",
  "verified": "jsdom A/B vs R48 on the 2026-08 exports (08/01-08/16, 2003 jobs, $2,799,603, GM 56.4%): gates 6/6 both; all 8 data markers byte-identical incl RAW (modulo builtAt); all 2003 per-job records identical; every summary key identical in all 3 scopes; flags 94 both; zero engine errors both; rendered panes differ only by the intended label text plus the build clock. DOM count: 94 approve-action buttons in both, all 94 relabelled, none left reading Approve. Functional 12/13 vs R48 11/13 - the single shared failure is a harness artifact present in BOTH engines, and R48 additionally fails its own card-vs-grid label consistency (card read Undo approval, grid read Undo), which is what this revision fixes.",
  "frozen_scripts": "build.js, close.js, snap.js and ages.py run VERBATIM."
 },
 "r51": {
  "date": "2026-08-18",
  "summary": "HVAC purchase flags go spec-first via the Task-to-Purchase map (v3, 381 builds / 538 task codes, locked by Stephen 2026-08-18). Jobs whose invoice carries a mapped 'HVAC- Replacement' task are judged on the exact expected package instead of the % band: condenser/package tonnage must match the task EXACTLY; coil/furnace/air-handler may upsize vs the condenser, never downsize; each piece checks against its category's priciest acceptable option; the package checks against band-high (with-coil band applied when a coil was bought); Material-typed PO total checks against the map's mat band with calibration dials in the map file (cal.matMult default 2.5, cal.matFloor default $1000 — the raw p75 band flagged 88% of June's mapped jobs, the calibrated line flags the true tail at ~14%). All checks remain the ONE PURCH-CAP flag with reason bubbles +EQ / TON / $HI / MAT; legacy band jobs show a % or $ bubble. Two new markers: TPM (the map, from Claude Docs/Profit Planner/task_to_purchase_map_data.json, private-repo fallback tpm/) and EQPO (job-tagged equipment+rescue PO lines and per-job Material-typed PO sums from Databricks fact_purchase_order_item, window minus 30 days to window end plus 7). Null payloads = byte-identical flagging to R50 (mapped-task jobs fall back to the band; the bands tab and job card say so). Surfaces: cap-type bubbles on PURCH-CAP pills, muted job-cell markers (map = judged by map, map~ = mapped task but no PO lines so band fallback), 'Flags & purchase plan' block on the job card (plain-language reason per flag + expected-package vs bought-pieces tables with per-piece verdicts, returns caveat on +EQ), and the flag-bands tab map panel (coverage summary, per-type coverage, collapsible per-job table). AMENDED same-day pre-release (Stephen's catch): Lennox condenser tonnage parses from the TRAILING delimited/zero-led group (-024/-036/-048/-060), never the leading series number (EL18 = Elite 18 SEER, not 1.5T); Goodman split pairs accept the zero-led form (GSXN4|024|10); voltage suffixes (230/208/240/460/115/575) are stripped before the last-token fallback so -230 can never read 2.5T. 25-case parser battery all-pass; June A/B and spliced results reproduce identically (22 map flags, zero numeric diffs). RE-FREEZE 2026-08-19: the 2026-08-18 freeze accidentally shipped the PRE-amendment plaintext (sha 3e884ec5..., 319,873 chars) - caught live on job 554522 (EL18KCV-048-230 flagged 1.5T). This manifest + template.html now carry the amended plaintext; diff vs the mis-frozen copy is confined to tpmTons (17 lines), flags-only, no dollar can move.",
  "verify": "jsdom A/B vs R50 on the June 2026 exports (goals=skip): NULL payloads -> gates 6/6 both, 3,902 jobs, $6,087,636, GM 55.3%, window 06/01/26-06/30/26, flags 171=171 with zero diffs, zero per-job numeric diffs (17 fields), zero summary diffs in all 3 scopes, all 7 tab panes byte-identical modulo the rev stamp / build clock / intended bubble+tooltip+bands-panel markup; snap.js reproduces the ledger exactly (171 flagged, 126 HVAC / 45 Plumb, $299,421.09); close.js handoff byte-identical. SPLICED payloads (June window, real Genie pulls: 326 equipment/rescue lines on 236 jobs, 1,607 material sums) -> zero numeric diffs anywhere (the map moves flags only, never dollars); 168 jobs carried mapped tasks: 74 judged by the map, 94 honest band fallback (no equipment PO lines visible - warehouse-stock pulls or feed gap); +22 PURCH-CAP, ALL on map-mode jobs, zero unexplained flag diffs ($HI 13, MAT 10, TON 2, +EQ 2); snap.js on the spliced build: 192 flagged, PURCH-CAP 50->72. Functional smoke 21/21: bubbles render (map reasons + legacy %/$), map markers, card open/detail/close, bands panel (summary, type coverage, 168-row job table), save round-trip carries TPM+EQPO and recomputes identically with zero errors. build.js, close.js, snap.js, ages.py all run VERBATIM.",
  "expect_first_build": "With the map + PO payloads spliced, the flagged count RISES by the map findings (June replay: +22) and some previously %-flagged mapped jobs can unflag where the map says the package is inside bands - re-baseline the review queue, not the margin. No dollar figure can move: if revenue/GM/purchases change on identical inputs, that is NOT R51 - stop and flag. Unattended runs without stored Databricks-Genie approval skip the EQPO pull (mapped jobs then read map~ fallback) and flag it - never hang on a permission prompt."
 },
 "r52": {
  "date": "2026-08-19",
  "summary": "Furnace and air-handler tonnage parsing corrected to the real model structure (Stephen 2026-08-19, Goodman/Amana nomenclature sheets), plus the EFF AFUE-tier check. Furnaces are NOT rated in tonnage pairs: Goodman/Amana = prefix + stage (S/T, identification only) + AFUE pair (80/92/96/97) + BTU group (3 digits) + max-CFM digit (3=1200/4=1600/5=2000 CFM -> 3/4/5T; no half sizes - the furnace pairs UP to the condenser's half size, never down, which the engine's existing >=-floor downsize rule enforces once the parse is right); Lennox/Elite = trailing airflow pair after the BTU group (the leading number is the series/AFUE - ML180 is the 80% series, never 1.5T). Air handlers = nominal capacity after the alpha prefix (AMST36=3T, AHVE60=5T) or Lennox trailing zero-led group (CBK48MVT-030 = 2.5T; 48 is the series). R51 read the first tonnage-looking pair anywhere, so GR9S800603AN (80%/60k/3T) read 5T off the BTU digits and ML180UH090V48B read 1.5T - furnace downsizing was invisible. Unknown formats return null (fail-open); the text fallback also accepts '4.0 TON'. Condenser/package/coil parsing byte-unchanged from amended R51 (EL18KCV-048-230 -> 4T). NEW EFF bubble on PURCH-CAP: furnace AFUE tier (80 vs 90+, from the model) must match the sold task's tier (builds' eff field); a 90%+ furnace typically runs $800-$1,300 over the 80% version, so a tier miss is real money even inside a price band. Condenser-less builds (furnace-only/AH-only/coil-only tasks) now anchor the downsize floor on the build's own tonnage. Fur/AH classification prefix fallbacks gain GR9T/CBK/AHVE/AWUF. Bubbles are now +EQ / TON / EFF / $HI / MAT; stage (S vs T) is deliberately NOT flagged - price catches it via $HI.",
  "verify": "2026-08-19, jsdom, June 2026 exports through frozen build.js, goals=skip. 126/126 parser+eval regression battery (every furnace/AH/condenser/coil model in map v3, AFUE tiers, TON/EFF/upsize/anchor-floor scenarios, R51 condenser battery incl. EL18KCV-048-230 -> 4T). NULL-payload A/B vs amended R51: gates 6/6 both, 3,902 jobs / $6,087,636 / GM 55.3% / window 06/01/26-06/30/26; whole-output diff == template diff EXACTLY (builtAt + entity serialization normalized) - zero unintended movement. SPLICED A/B (map v3 381 builds/538 codes from SharePoint raw payload; real Genie EQPO pulls, 567 eq/rescue lines, 2,766 material-sum jobs, window 2026-05-02..2026-08-19): zero numeric diffs on all 3,902 jobs, zero flag-count deltas (193 flagged both), zero map-mode changes; 22 real June pieces receive corrected tonnage (GRVT800805CN null->5T, GR9S/GRVT960603BN 5T->3T, ML296UH070XV36BK ->3T); reason delta exactly ONE: job 545261 (ML296 96%-AFUE furnace on an 80% build, already $HI $493-over) additionally names EFF. Save round-trip: saved copy carries TPM+EQPO and recomputes identical flags/GM/reasons on all jobs, zero engine errors. build.js, close.js, snap.js, ages.py VERBATIM (build.js run live this session).",
  "expect_first_build": "Flags-only, and in practice nearly invisible on current data: June replay moves exactly one reason bubble (EFF on 545261). The real payoff is forward-looking - wrong-size furnaces/AHs and AFUE-tier swaps were previously unflaggable and now read TON/EFF. No dollar figure can move: if revenue/GM/purchases change on identical inputs, that is NOT R52 - stop and flag."
 },
 "r53": {
  "summary": "Reviews name the error type — Review button (Jobs grid + job card) opens the What-was-the-error-type popup: Discounting / Training / Price / No Issue. Picking one marks the job Reviewed (same approve op, rec.cat added) and files it into that action list; three list view pills beside Reviewed and Watchlist; Recategorize via new recat op; Review Desk queue cards count the lists. Display/state-layer only — with builtAt normalized the whole June nightly output diff equals the template diff exactly: 3,902 jobs / $6,087,636 / GM 55.3% / gates 6/6 / RAW and all data markers byte-identical. Pre-R53 reviews carry no category, render a Categorize button, and can be filed later; state.json shape and STX protocol unchanged."
 },
 "r54": {
  "summary": "Display-only, three surfaces. (1) GM Review Desk gains a 14-day ACTIVITY FEED (every recorded action from the shared log — reviews with the error type currently on the record, recategorizations, watch adds/removes, manual and auto re-flags, notes — jobs outside the loaded data month render a plain job number) and the PER-QUEUE JOB LISTS: Watchlist + the three action lists (Training / Price / Discounting), each a table (job link, trade, type, revenue, GM%, by/when + category pill) sorted newest-first. Grid watch/review actions repaint the desk on tab open (unchanged R45 behavior); recordReview repaints immediately. (2) Flag Bands — HVAC gains the map BUILD CARDS under the % band section: every Task-to-Purchase build as a card — preferred unit (IDEAL/SPEC) per component, acceptable subs with unit costs, package equipment cost band (incl. with-coil), materials band with the calibrated MAT flag line (hi×matMult or hi+matFloor) — grouped by star tier then system family, collapsible, with a search box over task code / name / tonnage / model (empty groups hide while searching). Renders from PP_TPM only (map not loaded → no cards, same notice as R51). (3) Instructions tab REWRITTEN around the operating loop: what the tool forecasts (margin/GP/CP + month-end prediction, non-revenue erosion as lost capacity in opportunity), what flags and why (COM-OVER, labor over plan, PURCH-CAP bubbles TON/EFF/$HI/+EQ/MAT, NEG/LOW-GM), the margin standards to research against (55% replacement / 60% repair — operational standards; the hard LOW-GM line stays 40%), the daily routine (log in daily, research each flagged job, fix errors, categorize Discounting / Training / Price / No Issue), the GM desk, forecast mechanics condensed, month-end handoff, housekeeping. NOTHING behavioral changed: parsers, computeAll, flags, state ops (approve/recat/watch/…), state.json shape, STX sync, checkReflags, buildSaveHtml machinery all untouched — the new surfaces are pure reads over ST/PP_TPM.",
  "verify": "2026-08-19, jsdom, June 2026 exports through frozen build.js, goals=skip. A/B vs R53: gates 6/6 both, 3,902 jobs / $6,087,636 / GM 55.3% / window 06/01/26-06/30/26; ALL 10 data markers byte-identical incl. RAW; with builtAt normalized and entities DOM-canonicalized the whole 1.59 MB output diff equals the template diff EXACTLY (-14/+124 lines, line-for-line). Functional smoke 35/35 on the built output with the real TPM v3 payload spliced (381 cards render, counts/search/group-collapse verified; review→Training files the job and the feed shows it; recat→Price moves the lists; watch repaints on desk-tab open; occurrence-count proof no live popup serializes; save round-trip re-renders feed, lists and cards from embedded STATE/TPM with zero engine errors). build.js, close.js, snap.js, ages.py VERBATIM (build.js run live).",
  "expect_first_build": "Nothing moves — no dollar, no flag. The desk simply shows more, the bands tab documents the map, and the Instructions read current."
 }
}